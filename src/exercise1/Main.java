package exercise1;

import java.util.Scanner;

import static exercise1.Methods.*;

public class Main {
    public static void main(String[] args) {
       /* System.out.println(getTrue());
        System.out.println(getChar(3));


        Methods zero=new Methods();
        Scanner scan = new Scanner(System.in);
        int nr1 = Integer.parseInt(scan.nextLine());
        System.out.println(zero.compareZero(nr1));

        Methods even=new Methods();
        int nr2 = Integer.parseInt(scan.nextLine());
        System.out.println(even.evenOrNot(nr2));


        Methods ch=new Methods();
        String text = (scan.nextLine());
        System.out.println(ch.tenCharacters(text));

        Methods aChar=new Methods();
        System.out.println(aChar.getaChar());

        Methods charAt1=new Methods();
        Scanner scan = new Scanner(System.in);
        String text1 = (scan.nextLine());
        System.out.println("the first character is: "+charAt1.firstChar(text1));

        Methods oneCh=new Methods();
        Scanner scan = new Scanner(System.in);
        String text2 = (scan.nextLine());

        System.out.println("which character do you want to print out:");
        int nr3 = Integer.parseInt(scan.nextLine());
        System.out.println("the character is: "+ oneCh.whichChar(text2,nr3));

        //Använd förra metoden för att skriva ut alla bokstäver i en String en efter en

        Methods allCh = new Methods();
        Scanner scan = new Scanner(System.in);
        String text3 = (scan.nextLine());

        allCh.allChar(text3);*/

        //Skapa en metod som returnerar en array med de första tio heltalen

        System.out.println("Write the order of fibonacci you want:");

    }


}
